1，下载wamp，并安装软件。
2，将vote2解压至wamp目录下的www文件夹下。
3，在浏览器输入http://127.0.0.1/phpmyadmin
4，新建一个数据库，名称为my_tp
5，将vote2下的my_tp.sql导入到数据库中。
6，修改conn.php的参数，数据库用户名，密码之类的
7，在浏览器输入http://localhost/vote2/ztlist.php

然后基础环境就搭配好了
一定要注意文件路径。